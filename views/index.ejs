<%- include("templates/header") %>
<div class="container py-4">
  <%- include("templates/searchBar") %>
  <div class="recommend py-5">
    <div
      class="d-inline-flex justify-content-center px-4 py-2 bd-highlight fs-3 rounded-pill bg-success"
      style="--bs-bg-opacity: 0.3"
    >
      For -username- <%= username %> (Recommendations)
    </div>
  </div>

  <div class="container">
    <div class="row">
      <% randomRecipes.forEach(function(recipe) { %>
        <div class="col-md-4 mb-3">
          <div class="card">
            <!-- <img src="/path/to/images/<%= recipe.Image_Name %>" class="card-img-top" alt="Recipe Image"> -->
            <div class="card-body d-flex flex-column">
              <h5 class="card-title"><%= recipe.Title %></h5>
              <!-- <h6>Ingredients</h6>
              <% const ingredients = recipe.Ingredients.replace(/\[|\]|'/g, '').split(','); %>
              <% ingredients.forEach(function(ingredient) { %>
              <br><%= ingredient.trim() %>
              <% }); %>
              <% console.log(recipe.Ingredients) %> -->
              <div class="mt-auto align-self-end">
              <a href="/bookmarked" class="bookmark-icon">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-bookmark" viewBox="0 0 16 16">
                  <path d="M2 2a2 2 0 0 1 2-2h8a2 2 0 0 1 2 2v13.5a.5.5 0 0 1-.777.416L8 13.101l-5.223 2.815A.5.5 0 0 1 2 15.5V2zm2-1a1 1 0 0 0-1 1v12.566l4.723-2.482a.5.5 0 0 1 .554 0L13 14.566V2a1 1 0 0 0-1-1H4z"/>
                </svg>
              </a>
              </div>
            </div>
          </div>
        </div>
      <% }); %>
    </div>
  </div>

  </div>
  <br />
</div>
<%- include("templates/footer") %>
